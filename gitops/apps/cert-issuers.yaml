apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: yaook-cert-issuers
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: yaook
  source:
    repoURL: __REPO_URL__
    targetRevision: __TARGET_REVISION__
    path: manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: yaook
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
